# campuses & students — client

frontend for the campuses & students crud app. csci 39548 final project.

## stack

- react 19 + typescript
- vite
- tailwind css

## running locally

```bash
npm install
npm run dev
```

dev server runs on http://localhost:5173.

## scripts

- `npm run dev` — start the dev server
- `npm run build` — production build
- `npm run preview` — preview the production build

## deployed url

tbd (vercel)
